// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build
export default defineConfig({
  // ⚠️ Mets ici l'URL finale de ton site (utilisée pour le SEO et les URLs canoniques)
  site: 'https://www.nessia.eu',
});
